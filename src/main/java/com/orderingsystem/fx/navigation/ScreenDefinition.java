package com.orderingsystem.fx.navigation;

/**
 * Một mục menu trong shell — FXML + văn bản trợ giúp ngữ cảnh (Nielsen #10).
 */
public record ScreenDefinition(String label, String fxmlClasspath, String helpText) {
}
